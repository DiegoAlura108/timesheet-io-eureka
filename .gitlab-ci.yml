mage: java:11

before_script:
  - chmod +x mvnw

stages:
  - staging
  
staging:
  stage: deploy
  script: 
    - ./mvnw package
    - dpl --provider=heroku --app=$HEROKU_APP_STAGING --api-key=$HEROKU_API_KEY
  artifacts:
    paths:
      - target/eureka-0.0.1-SNAPSHOT.jar
  only:
    - master